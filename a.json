<ScrollView horizontal={true}>
                    {[...Array(Math.ceil(item.items.length / 2))].map((_, colIndex) => (
                        <View key={colIndex} style={{ flexDirection: 'column' }}>
                            {/* Map through the items in each column */}
                            {item.items.slice(colIndex * 2, colIndex * 2 + 2).map((offer, rowIndex) => (
                                <View key={`${colIndex}-${rowIndex}`} style={{
                                    borderRadius: 10,
                                    marginRight: 20,
                                    marginBottom: rowIndex === 0 ? 10 : 0, // Add margin bottom for the first item in each column
                                    width: offer.tileSize === "Size_3_x_2" ? 340 : offer.tileSize === "Size_2_x_2" ? 300 : offer.tileSize === "Size_1_x_2" ? 200 : 200,
                                    height: offer.tileSize === "Size_3_x_2" ? 340 : offer.tileSize === "Size_2_x_2" ? 350 : offer.tileSize === "Size_1_x_2" ? 340 : 200,
                                    overflow: 'hidden',
                                    backgroundColor: '#191919',
                                }}>
                                    {/* <AutoScrollingScrollView width={offer.tileSize === "Size_2_x_2" ? 300 : offer.tileSize === "Size_1_x_2" ? 150 : 200} height={offer.tileSize === "Size_2_x_2" ? 300 : offer.tileSize === "Size_1_x_2" ? 150 : 200} images={offer.displayAssets.map(e => {
                                            return e.background
                                        })}/> */}

                                </View>
                            ))}
                        </View>
                    ))}

                </ScrollView>